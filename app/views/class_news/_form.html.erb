<%= form_with(model: class_news, local: true) do |form| %>
  <% if class_news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(class_news.errors.count, "error") %> prohibited this class_news from being saved:</h2>

      <ul>
      <% class_news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Titulo %>
    <%= form.text_area :title, id: :class_news_title, :cols => 50 %>
  </div>

  <div class="field">
    <%= form.label :Bajada %>
    <%= form.text_area :drop, id: :class_news_drop, :rows => 10, :cols => 50 %>
  </div>

  <div class="field">
    <%= form.label :Cuerpo %>
    <%= form.text_area :body, id: :class_news_body, :rows => 40, :cols => 50 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
